name: Reusable stable qualification

on:
  workflow_call:
  workflow_dispatch:

permissions: {}

env:
  CANDIDATE_SHA: ${{ github.event.pull_request.head.sha || github.sha }}
  SETUPTOOLS_SCM_PRETEND_VERSION: "0.1.1"
  UV_NO_SOURCES: "1"
  UV_VERSION: "0.10.9"

jobs:
  source-quality:
    name: Source quality (Python ${{ matrix.python-version }})
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.12", "3.13", "3.14"]
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
          ref: ${{ env.CANDIDATE_SHA }}
      - uses: astral-sh/setup-uv@20cfd1bf945f4377ade1205e4dbc17946fc9a30d # v10.0.1
        with:
          version: ${{ env.UV_VERSION }}
      - run: uv python install ${{ matrix.python-version }}
      - run: uv sync --python ${{ matrix.python-version }} --all-extras --dev --locked
      - run: uv run python scripts/qualification/run_stable_gate.py --group source

  deterministic:
    name: Deterministic tests (Python ${{ matrix.python-version }})
    runs-on: ubuntu-latest
    timeout-minutes: 20
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.12", "3.13", "3.14"]
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
          ref: ${{ env.CANDIDATE_SHA }}
      - uses: astral-sh/setup-uv@20cfd1bf945f4377ade1205e4dbc17946fc9a30d # v10.0.1
        with:
          version: ${{ env.UV_VERSION }}
      - run: uv python install ${{ matrix.python-version }}
      - run: uv sync --python ${{ matrix.python-version }} --all-extras --dev --locked
      - run: uv run python scripts/qualification/run_stable_gate.py --group deterministic

  dependency:
    name: Dependency audit and compatibility
    runs-on: ubuntu-latest
    timeout-minutes: 20
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
          ref: ${{ env.CANDIDATE_SHA }}
      - uses: astral-sh/setup-uv@20cfd1bf945f4377ade1205e4dbc17946fc9a30d # v10.0.1
        with:
          version: ${{ env.UV_VERSION }}
      - run: uv python install 3.12
      - run: uv sync --python 3.12 --all-extras --dev --locked
      - run: uv run python scripts/qualification/run_stable_gate.py --group dependency

  stress:
    name: Stress and controlled faults
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
          ref: ${{ env.CANDIDATE_SHA }}
      - uses: astral-sh/setup-uv@20cfd1bf945f4377ade1205e4dbc17946fc9a30d # v10.0.1
        with:
          version: ${{ env.UV_VERSION }}
      - run: uv python install 3.12
      - run: uv sync --python 3.12 --all-extras --dev --locked
      - run: uv run python scripts/qualification/run_stable_gate.py --group stress

  performance:
    name: Sustained performance qualification
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
          ref: ${{ env.CANDIDATE_SHA }}
      - uses: astral-sh/setup-uv@20cfd1bf945f4377ade1205e4dbc17946fc9a30d # v10.0.1
        with:
          version: ${{ env.UV_VERSION }}
      - run: uv python install 3.12
      - run: uv sync --python 3.12 --all-extras --dev --locked
      - run: >-
          uv run python scripts/qualification/run_stable_gate.py
          --group performance
          --evidence-directory "${{ runner.temp }}/ml4t-live-performance"
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: performance-${{ env.CANDIDATE_SHA }}
          path: ${{ runner.temp }}/ml4t-live-performance/performance-qualification.json
          if-no-files-found: error
          retention-days: 30

  documentation:
    name: Strict documentation
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
          ref: ${{ env.CANDIDATE_SHA }}
      - uses: astral-sh/setup-uv@20cfd1bf945f4377ade1205e4dbc17946fc9a30d # v10.0.1
        with:
          version: ${{ env.UV_VERSION }}
      - run: uv python install 3.12
      - run: uv sync --python 3.12 --all-extras --dev --locked
      - run: uv run python scripts/qualification/run_stable_gate.py --group documentation

  build:
    name: Build fixed candidate
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: read
    needs:
      - source-quality
      - deterministic
      - dependency
      - stress
      - performance
      - documentation
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
          ref: ${{ env.CANDIDATE_SHA }}
      - uses: astral-sh/setup-uv@20cfd1bf945f4377ade1205e4dbc17946fc9a30d # v10.0.1
        with:
          version: ${{ env.UV_VERSION }}
      - run: uv python install 3.12
      - run: uv sync --python 3.12 --all-extras --dev --locked
      - name: Verify non-release tags cannot set the package version
        run: |
          env -u SETUPTOOLS_SCM_PRETEND_VERSION uv build \
            --wheel \
            --out-dir "${RUNNER_TEMP}/vcs-version" \
            --build-constraints build-constraints.txt
          uv run python scripts/qualification/verify_vcs_version.py \
            --artifacts-dir "${RUNNER_TEMP}/vcs-version" \
            --expected-version "${SETUPTOOLS_SCM_PRETEND_VERSION}"
      - name: Build from the fixed commit timestamp
        run: |
          export SOURCE_DATE_EPOCH
          SOURCE_DATE_EPOCH=$(git show -s --format=%ct HEAD)
          uv build --build-constraints build-constraints.txt
      - run: uv run twine check --strict dist/*
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: dist-${{ env.CANDIDATE_SHA }}
          path: dist/
          if-no-files-found: error
          retention-days: 30

  artifact-qualification:
    name: Exact installed artifact qualification
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      actions: read
      contents: read
    needs: build
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
          ref: ${{ env.CANDIDATE_SHA }}
      - uses: astral-sh/setup-uv@20cfd1bf945f4377ade1205e4dbc17946fc9a30d # v10.0.1
        with:
          version: ${{ env.UV_VERSION }}
      - run: uv python install 3.12 3.13 3.14 3.15
      - run: uv sync --python 3.12 --all-extras --dev --locked
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: dist-${{ env.CANDIDATE_SHA }}
          path: dist/
      - name: Qualify the uploaded artifact without rebuilding for promotion
        run: >-
          uv run python scripts/qualification/qualify_artifacts.py
          --artifacts-dir dist
          --output artifact-qualification.json
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: qualification-${{ env.CANDIDATE_SHA }}
          path: artifact-qualification.json
          if-no-files-found: error
          retention-days: 30

  security:
    name: Security, provenance inputs, and recovery
    runs-on: ubuntu-latest
    timeout-minutes: 20
    permissions:
      actions: read
      contents: read
    needs: build
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
          ref: ${{ env.CANDIDATE_SHA }}
      - uses: astral-sh/setup-uv@20cfd1bf945f4377ade1205e4dbc17946fc9a30d # v10.0.1
        with:
          version: ${{ env.UV_VERSION }}
      - run: uv python install 3.12
      - run: uv sync --python 3.12 --all-extras --dev --locked
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: dist-${{ env.CANDIDATE_SHA }}
          path: dist/
      - name: Qualify exact-artifact security and provenance inputs
        run: >-
          uv run python scripts/qualification/qualify_security.py
          --artifacts-dir dist
          --output-directory security-evidence
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: security-${{ env.CANDIDATE_SHA }}
          path: security-evidence/
          if-no-files-found: error
          retention-days: 30
